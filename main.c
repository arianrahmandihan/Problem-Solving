
#include <stdio.h>
#include <string.h>
int match(char [], char []);

int main() {
  char a[100], b[100];
  int position;

  char char1= 'abc';

  printf("Enter a string to find\n");
  gets(b);



  return 0;
}

int match(char text[], char pattern[]) {
  int c, d, e, text_length, pattern_length, position = -1;

  text_length    = strlen(text);
  pattern_length = strlen(pattern);

  if(match(a,b){
    printf("this is mathced");


  }



  return -1;
}
